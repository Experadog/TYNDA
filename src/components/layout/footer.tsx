import { FC } from 'react';

interface IProps {
    viewModel: ViewModel['Layout']['footer'];
}

const Footer: FC<IProps> = ({ viewModel }) => {
    return <footer></footer>;
};

export default Footer;

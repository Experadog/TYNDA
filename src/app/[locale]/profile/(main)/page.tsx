import { FC } from 'react';
import MainProfileView from './view/main-profile-view';

interface IProps {}

const ProfileMain: FC<IProps> = async ({}) => {
    return <MainProfileView />;
};

export default ProfileMain;

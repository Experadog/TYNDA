import { FC } from 'react';
import UpdateProfileInfoView from './view/update-profile-info-view';

interface IProps {}

const UpdateInfo: FC<IProps> = ({}) => {
    return <UpdateProfileInfoView />;
};

export default UpdateInfo;

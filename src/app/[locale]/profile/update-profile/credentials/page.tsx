import { FC } from 'react';
import UpdateProfileCredentialsView from './view/update-profile-credentials-view';

interface IProps {}

const ProfileCredentials: FC<IProps> = ({}) => {
    return <UpdateProfileCredentialsView />;
};

export default ProfileCredentials;
